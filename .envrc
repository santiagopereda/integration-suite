# Claude Code Telemetry Configuration
# Use with direnv (https://direnv.net/) for automatic environment setup
#
# To enable:
#   1. Install direnv: sudo apt install direnv (or brew install direnv)
#   2. Add to shell: eval "$(direnv hook bash)" (or zsh)
#   3. Allow this file: direnv allow

# Enable Claude Code native OpenTelemetry telemetry
export CLAUDE_CODE_ENABLE_TELEMETRY=1

# OTel collector endpoint (start with: cd .agent/telemetry/otel && docker compose up -d)
export OTEL_EXPORTER_OTLP_ENDPOINT="http://localhost:4317"

# Service name for this project (appears in telemetry data)
export OTEL_SERVICE_NAME="claude-code-agentic"
